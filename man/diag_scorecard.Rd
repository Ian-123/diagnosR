% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diag_scorecard.R
\name{diag_scorecard}
\alias{diag_scorecard}
\title{One-page diagnostic scorecard for lm models (with influential rows)}
\usage{
diag_scorecard(
  model,
  vif_threshold = 10,
  kappa_threshold = 30,
  include_bp = FALSE,
  include_reset = FALSE,
  lb_lags = 1:4,
  export_csv = NULL,
  show_influence_rows = TRUE,
  max_rows_to_print = 10
)
}
\arguments{
\item{model}{Fitted \code{lm} object.}

\item{vif_threshold}{Flag if adjusted VIF > this (default 10).}

\item{kappa_threshold}{Flag if condition number >= this (default 30).}

\item{include_bp}{Logical; if TRUE, runs Breusch-Pagan test. Default FALSE.}

\item{include_reset}{Logical; if TRUE, runs Ramsey RESET test. Default FALSE.}

\item{lb_lags}{Integer vector of lags for Ljung-Box fallback (used inside autocorr).}

\item{export_csv}{Optional file path to save the scorecard table as CSV.}

\item{show_influence_rows}{Logical; if TRUE, prints row indices over the Cook's D cutoff.}

\item{max_rows_to_print}{Max influential row indices to print inline. Default 10.}
}
\value{
Invisibly, a list with \code{table}, \code{influential_rows}, and cutoff.
}
\description{
One-page diagnostic scorecard for lm models (with influential rows)
}
